import { NextResponse } from 'next/server';
import { auth } from '@/lib/auth';
import { db } from '@/lib/db';
export async function GET(_request: Request, { params }: { params: { slug: string } }): Promise<NextResponse> { try { const data = await db.collection.findUnique({ where: { slug: params.slug }, include: { author: { select: { id: true, name: true, image: true } }, projects: { orderBy: { addedAt: 'desc' }, include: { project: { select: { id: true, slug: true, title: true, shortDescription: true, tags: true, language: true, license: true, starsCount: true } } } } } }); if (!data || !data.isPublic) return NextResponse.json({ error: 'Collection not found.' }, { status: 404 }); return NextResponse.json({ data }); } catch { return NextResponse.json({ error: 'Unable to load collection.' }, { status: 500 }); } }
export async function PUT(request: Request, { params }: { params: { slug: string } }): Promise<NextResponse> { try { const session = await auth(); if (!session) return NextResponse.json({ error: 'Authentication required.' }, { status: 401 }); const collection = await db.collection.findUnique({ where: { slug: params.slug } }); if (!collection || collection.authorId !== session.user.id) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); const body = await request.json() as { name?: string; description?: string; isPublic?: boolean }; const data = await db.collection.update({ where: { id: collection.id }, data: { name: body.name?.trim() || undefined, description: body.description?.trim() || undefined, isPublic: body.isPublic } }); return NextResponse.json({ data }); } catch { return NextResponse.json({ error: 'Unable to update collection.' }, { status: 500 }); } }
export async function DELETE(_request: Request, { params }: { params: { slug: string } }): Promise<NextResponse> { try { const session = await auth(); if (!session) return NextResponse.json({ error: 'Authentication required.' }, { status: 401 }); const collection = await db.collection.findUnique({ where: { slug: params.slug } }); if (!collection || collection.authorId !== session.user.id) return NextResponse.json({ error: 'Forbidden.' }, { status: 403 }); await db.collection.delete({ where: { id: collection.id } }); return NextResponse.json({ data: { deleted: true } }); } catch { return NextResponse.json({ error: 'Unable to delete collection.' }, { status: 500 }); } }
