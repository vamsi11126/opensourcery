'use client'; import { Button } from '@/components/ui/button'; export default function ErrorPage({ reset }: { reset: () => void }): React.JSX.Element { return <main className="container py-24 text-center"><h1 className="text-3xl font-black">Something went wrong</h1><p className="mt-3 text-slate-600">Please try again.</p><Button className="mt-6" onClick={reset}>Try again</Button></main>; }
