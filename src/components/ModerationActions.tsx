'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { Button } from '@/components/ui/button';
export function ModerationActions({ projectId }: { projectId: string }): React.JSX.Element { const router = useRouter(); const [pending, setPending] = useState(false); async function act(action: 'approve' | 'reject' | 'flag'): Promise<void> { setPending(true); await fetch(`/api/admin/projects/${projectId}/approve`, { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ action }) }); router.refresh(); setPending(false); } return <div className="flex gap-2"><Button disabled={pending} onClick={() => void act('approve')}>Approve</Button><Button disabled={pending} variant="danger" onClick={() => void act('reject')}>Reject</Button><Button disabled={pending} variant="outline" onClick={() => void act('flag')}>Flag</Button></div>; }
